<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.signin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:label="签了老弟"
        android:theme="@style/AppTheme">
        <activity
            android:name=".logIn"
            android:label="This is login"></activity>
        <activity
            android:name=".MainActivity"
            android:label="SignIn"
            android:theme="@style/ToolBarTheme">
        </activity>
        <activity
            android:name=".SignIn"
            android:label="this is signIn" />
        <activity android:name=".SearchClass" />
        <activity android:name=".editStudentInfo" />
        <activity android:name=".SignIn2" />
        <activity android:name=".studentEnterClass"
                   android:theme="@style/ToolBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>